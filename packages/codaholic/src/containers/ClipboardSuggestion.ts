import { connect } from "react-redux";
import ClipboardSuggestion from "../components/organisms/ClipboardSuggestion";

const mapStateToProps = () => ({});
const mapDispatchToProps = () => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(ClipboardSuggestion);
